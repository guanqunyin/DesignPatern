1. 需求： 前台来的请求必须经过我得过个过滤链才能通过

java servelet API 规定了 请求必须经过 1，2，3然后相应必须经过3，2，1。

request ——> A ——> B ——> A ——>回到前台

模拟了以下步骤来实现：

        //1.新建一个请求和一个响应

        //1.1 模拟多个请求


        //2.新建一个链子

        //3. 把所有的过滤器加入链子中

        //4. 当请求来临的时候，执行每一个链子的dofilter方法， 并一步步返回.